class AppConstants {
  static const String appName = 'SafeRoute AI';
  static const Duration splashDuration = Duration(seconds: 2);
  static const Duration authTransitionDuration = Duration(milliseconds: 900);
  static const Duration reportSubmitDelay = Duration(milliseconds: 1200);
}
